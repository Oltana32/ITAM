Place the login background image here so the app can use it.

Filename expected by the app: `awash-login-bg.jpg`

Steps:
1. Save your attached image as `awash-login-bg.jpg`.
2. Put the file in this folder: `frontend/public/`.
3. Restart the dev server if it's running.

The login page uses the image at `/awash-login-bg.jpg` as the background.
