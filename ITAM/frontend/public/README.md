Place the login background image here so the app can use it.

Filename expected by the app: `awash-login-bg.png`

Steps:
1. Save your design mockup as `awash-login-bg.png`.
2. Put the file in this folder: `frontend/public/`.
3. Restart the dev server if it's running.

The login page uses the image at `/awash-login-bg.png` as the full-page background.
